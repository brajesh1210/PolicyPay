{
  "name": "policypay",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "frontend",
    "demo-agent",
    "packages/*"
  ],
  "dependencies": {
    "@policypay/contracts": "^1.0.0"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.10"
  },
  "scripts": {
    "build": "npm run build --workspace @policypay/contracts && npm run build --workspace backend",
    "start": "npm run start --workspace backend"
  }
}